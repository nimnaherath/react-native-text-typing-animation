import { StyleSheet } from 'react-native';

import { DEFAULT_FONT_SIZE, DEFAULT_FONT_WEIGHT } from './constants';

export const styles = StyleSheet.create({
  text: {
    fontSize: DEFAULT_FONT_SIZE,
    fontWeight: DEFAULT_FONT_WEIGHT,
  },
});
